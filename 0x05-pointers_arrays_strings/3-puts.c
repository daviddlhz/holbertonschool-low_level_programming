#include "holberton.h"
/**
 * _puts - check the code for Holberton School students.
 * @str: is the parameter.
 * Return: Always 0.
 */
void _puts(char *str)
{
	int i, len;

	for (len = 0; s[len] != 0; len++)
	{
	}
	for (i = 0; i <= len; i++)
	{
		_putchar(str[i]);
	}
}
