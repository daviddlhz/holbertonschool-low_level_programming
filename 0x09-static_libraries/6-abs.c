#include "holberton.h"
/**
 * _abs - check the code for Holberton School students.
 *@abs: this parameter in the function
 * Return: Always 0.
 */
int _abs(int abs)
{
	if (abs >= 0)
	{
		return (abs);
	}
	else
	{
		return (-abs);
	}

}
